<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.tec.bamx_almacenista">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BAMX_almacenista">
        <activity
            android:name=".Login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DonativoEntregado"
            android:parentActivityName=".ProximasEntregas"/>

        <activity
            android:name=".ProximasEntregas"
            android:parentActivityName=".DonativoEntregado"/>

        <activity
            android:name=".Detalle_Entrega"
            android:parentActivityName=".ProximasEntregas"/>

        <activity
            android:name=".DonativoEditable"
            android:parentActivityName=".DonativoEntregado"/>

    </application>

</manifest>